
# Se eu salvar as credenciais do AWS neste arquivo, elas vão ficar expostas no github
# Por este motivo, deixei as linhas abaixo em formato de comentários
# Essas credenciais podem ser configuradas como "environment variables" tanto no Intellij quanto no Heroku
# Tutorial para salvar "environment variables" no heroku: https://catalins.tech/heroku-environment-variables

#aws_access_key_id = nao informar a sua chave aqui, ele le a do aws cli
#aws_secret_access_key = nao informar a chave aqui, só o nome do campo
#amazon.dynamodb.endpoint=http://localhost:8000

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
springdoc.swagger-ui.path=/swagger-ui-heroes-reactive-api.html